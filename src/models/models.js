export class UserModel {
  constructor(first_name, last_name, email, mobile, password) {
    this.firstName = first_name;
    this.lastName = last_name;
    this.email = email;
    this.mobile = mobile;
    this.password = password;
  }
}
